export const config = {
    BASE_URL: "http://localhost:3000",
    // BASE_URL: "http://host.docker.internal:3000" (caso esteja utilizando o docker no Windows em ambiente de desenvolvimento),
    // BASE_URL: "http://node_api:3000" (caso esteja utilizando o docker no Linux em ambiente de desenvolvimento),
};